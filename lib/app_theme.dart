import 'package:flutter/material.dart';

const EASYLIST_APP_BLUE = Color.fromRGBO(23, 72, 105, 1.0);

final appTheme = ThemeData(
    brightness: Brightness.light,
    primaryColor: EASYLIST_APP_BLUE,
    accentColor: Colors.pink);
